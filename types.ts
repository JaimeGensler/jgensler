export interface PostMetadata {
    title: string;
    subtitle: string;
    date: string;

    published: boolean;

    coverPhoto: string;
    coverPhotoAltText: string;
    __resourcePath: string;
}
